## 清掃員の人数
営業時間10:00~20:00(フードコート, レストランのみ21:00まで)
11時から17時が一番多いかも
正月セール、夏休み(特に7月)、GW、年末年始、大型連休、セール期間
* 平日
	* 営業時間内は4
	* 営業時間外は3
* 土日祝
	* 営業時間内は5
	* 営業時間外は3
* 
## 次回までの改善点
### Team C
Item storage
Process flow
日毎、月毎、クォーター毎の利益を計上する
充電場所が必要→常時稼働させるにはシフト制？
前回までの進捗と今回の進捗を図表で示せるようにまとめた方がいいかも
### 先生からのコメント
**移動のみではなくて人を動かしたシミュレーションを作る**
**正しい時間でシミュレーションを行う**

## FlexSimについて
#### オブジェクト
+ Flow item: あるステーションからほかのステーションにシミュレーションモデルを移動するオブジェクト→皿
	+ 汁物、麺系と軽食系で分けた方がいいかも
+ Source : フローアイテムを作成する場所→机
+ キュー: 
+ プロセッサ: フローアイテムを処理する係: Relayと人
+ シンク: 
#### 3Dオブジェクトの接続

#### モデル実行

#### 顧客を追跡するためのリストを追跡
グローバルリスト: 複雑なフローを作成するためのツール

#### モデルのロジックを編集

#### 処理フローモデルを構築(重要)

## 🅰 方法1：アイテムの生成位置をランダムにする（出現位置ランダム）

### 方法：

- **複数のSourceを用意**（例：`Source1`, `Source2`, `Source3`）
    
- **Process Flow**や**FlexScript**で、ランダムにどのSourceで生成するか決定
    

### 例：Process Flowで制御する場合

1. `Token Generator`（定期的に生成）
    
2. `PickList` で `random(1,3)` のように条件分岐
    
3. 各分岐に `Create Object` アクティビティ
    
    - それぞれ `Source1`, `Source2`, `Source3` にリンク
        

### FlexScriptでのランダム生成（例：イベント内で）

cpp

コピーする編集する
```cpp
int index = random(1, 3); 
if (index == 1) {
node("/Source1")->createCopy(node("/ItemDefinition")); 
}
else if (index == 2{
node("/Source2")>createCopy(node("/ItemDefinition")); 
}
else{node("/Source3")->createCopy(node("/ItemDefinition"));
}
```
