## 環境
- [ ] OS: Ubuntu-22.04LTS(WSL2)  
- [ ] miniconda3
- [ ] Python: ~~3.8~~ → 普通にpythonの最新バージョンを入れた方がいい
- [ ] GPU: NVIDIA GeForce RTX 3070Ti Laptop GPU
- [ ] NVIDIA-SMI: 555.52.01
- [ ] Driver Version: 555.99
- [ ] CUDA: 12.5

## ROS2 install
[ROS2](https://docs.ros.org/en/humble/Installation/Alternatives/Ubuntu-Development-Setup.html)
[[ros2 install]]
[[]]
### ワークスペース

- [ ] ワークスペースディレクトリ作成＆ビルド
```bash
$ cd
$ mkdir -p ros2_ws/src
$ cd ros2_ws
$ sudo rosdep init
$ rosdep update
$ source /opt/ros/humble/setup.bash
$ colcon build
```

ミスってる可能性大

## Rasberry pi 4BのOS書き直し
- [ ] [RPI-imager](https://www.raspberrypi.com/software/)
```bash
$ sudo apt install rpi-imager
```

### Imaging driver
# 📌 Raspberry Pi：カメラ & オーディオストリーミング用ファームウェア設定（日本語まとめ）

## 1. OS の書き込み（Raspberry Pi Imager）
![[Screenshot from 2025-12-02 15-36-00.png]]

1. [ ] **Raspberry Pi Imager**で最新の 64bit Raspberry Pi OS を書き込む  
    OS カスタマイズ画面で以下を設定：

    - [ ] ホスト名：`imaging.local`
    - [ ] ユーザー名：`pi`
    - [ ] 任意のパスワード
    - [ ] WiFi 設定
    - [ ] ロケール設定（これを設定しないと WiFi が無効化される)

2. [ ] **Services** タブで以下を有効化：
    - [ ] `Enable SSH`
    - [ ] `Use password authentication`


3. [ ] 書き込み後、Raspberry Pi を起動し、PC と **Ethernet で接続**  
    PC 側の IPv4 を **Link-Local Only** に設定  
    → `ssh pi@imaging.local` でログイン可能


---

## 2. このパッケージのインストール

- [ ] オーディオストリーミングなしなら追加パッケージは不要。

1. [ ] 起動後、PC から以下を実行して Pi にコードをコピー：
```bash
bash bash_scripts/sync_code_to_pi.sh
```

2. [ ] Pi へ接続：
```bash
ssh pi@imaging.local
cd imaging_driver
sudo bash install.sh
```

これで、**起動時に自動でストリーミングが開始する systemd サービス**がインストールされる。

---

## 3. オーディオストリーミングを使う場合（任意）

- [ ] オーディオも配信する場合は、Pi 上で以下を実行（インターネットが必要）：
```bash
sudo apt-get install python3-pyaudio
```

---

## 4. ロボットステーション向けネットワーク設定

- [ ] Pi 上でネットワーク設定ツール起動：
```bash
sudo nmtui
```

- [ ] `Edit a connection` → `Wired Connection` を編集  
    - [ ] `IPv4 Configuration`：**Link-Local**
    - [ ] `IPv6 Configuration`：**Disabled**

---

## 5. RPi カメラモジュール v3（imx708）の使用

- [ ] V3 カメラが認識されない場合の確認：

1. [ ] カメラ一覧を確認：
```bash
libcamera-hello --list-cameras
```

2. [ ] 認識されていない場合、設定ファイル編集：
```bash
sudo nano /boot/firmware/config.txt
```

- [ ] `camera_auto_detect=0` を **1 に変更**
- [ ] `[all]` セクションに以下を追加：

### Raspberry Pi 5 の場合（2ポート有り）

- [ ] `dtoverlay=imx708,cam1`  # cam1 に v3 カメラ
- [ ] `dtoverlay=ov5647,cam0`  # cam0 に v1 カメラ

### Pi 4 以前（1ポートのみ）の場合

- [ ] `dtoverlay=imx708`

---

## 6. （任意）V3 カメラモジュールのハードウェア WDR 有効化

WDR = 広ダイナミックレンジ（HDR のような効果）

1. [ ] カメラのデバイス名を確認：
```bash
v4l2-ctl -d /dev/v4l-subdev0 -l
```

- [ ] `wide_dynamic_range` を含むデバイス例：`/dev/v4l-subdev5`

2. [ ] WDR の ON / OFF

### ON（有効化）
```bash
v4l2-ctl --set-ctrl wide_dynamic_range=1 -d /dev/v4l-subdev5
```

### OFF（無効化）
```bash
v4l2-ctl --set-ctrl wide_dynamic_range=0 -d /dev/v4l-subdev5
```

---

## GPU(再)
- [x] 一応[NVIDIA-HP Drivers](https://www.nvidia.com/ja-jp/drivers/)で検索  
  nvidia-driver-580
- [x] インストール
```bash
$ sudo add-apt-repository ppa:graphics-drivers/ppa
$ sudo apt update
$ sudo apt install nvidia-driver-580
## pass: hayashi-dell
$ sudo reboot
```
- [x] 確認
```bash
$ nvidia-smi
```